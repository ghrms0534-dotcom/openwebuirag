# Open WebUI RAG - 폐쇄망 모드

# openssl rand -hex 32 로 생성한 값을 입력합니다.
WEBUI_SECRET_KEY=<openssl-rand-hex-32로-생성>
ENV=prod

# Ollama: 로컬 실행 또는 SSH 터널(localhost:11434)
ENABLE_OLLAMA_API=true
OLLAMA_BASE_URL=http://host.docker.internal:11434

# vLLM / OpenAI 호환 API
ENABLE_OPENAI_API=true
OPENAI_API_BASE_URL=http://host.docker.internal:8000/v1
OPENAI_API_KEY=dummy-key-for-local-vllm

# PostgreSQL
POSTGRES_PASSWORD=openwebui
DATABASE_URL=postgresql://openwebui:openwebui@postgres:5432/openwebui

# Qdrant
VECTOR_DB=qdrant
QDRANT_URI=http://qdrant:6333

# HuggingFace
HF_HUB_OFFLINE=1

# RAG 임베딩
RAG_EMBEDDING_ENGINE=
RAG_EMBEDDING_MODEL=BAAI/bge-m3
RAG_EMBEDDING_MODEL_AUTO_UPDATE=false
RAG_EMBEDDING_MODEL_TRUST_REMOTE_CODE=true
RAG_RERANKING_MODEL=
RAG_TOP_K=8
RAG_TOP_K_RERANKER=8
ENABLE_RAG_HYBRID_SEARCH=true
RAG_RELEVANCE_THRESHOLD=0.1

# LLM 기본 파라미터
DEFAULT_TEMPERATURE=0.3
DEFAULT_TOP_P=0.9
DEFAULT_TOP_K=40
DEFAULT_MIN_P=0.05
DEFAULT_REPEAT_PENALTY=1.1
DEFAULT_FREQUENCY_PENALTY=0.1
DEFAULT_MAX_TOKENS=2048
DEFAULT_NUM_CTX=8192
DEFAULT_NUM_GPU=1
DEFAULT_KEEP_ALIVE=5m

# 문서 파싱
CONTENT_EXTRACTION_ENGINE=tika
TIKA_SERVER_URL=http://tika:9998

# 로깅
GLOBAL_LOG_LEVEL=INFO